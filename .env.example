# Catálogo (desarrollo local)
DATABASE_URL=postgresql://floit:floit@localhost:5432/floit_catalog
# Primera vez: crear esquema automáticamente (no usar en producción)
DATABASE_SYNC=true
# Sembrar datos demo de Caracas al arrancar el servicio (solo dev)
SEED_ON_BOOT=true

# URLs entre servicios (Next BFF y search → catalog)
CATALOG_SERVICE_URL=http://localhost:4010
SEARCH_SERVICE_URL=http://localhost:4011
